# 02 — Deviations

Measured against the Ka0s WoW Addon Standard **v2.7.0 (2026-07-17)**. One entry per MUST/SHOULD the addon fails or partially meets. Section references use the `filename-§N` scheme. IDs carry the `PC-` prefix; recurring deviations keep their 2026-07-12 IDs, new ones continue the sequence from `PC-30`.

**Legend — Status:** `open` = unaddressed; `documented` = an intentional deviation already recorded in `CLAUDE.md` (surfaced here for completeness so the user can re-confirm it against v2.7.0).

| ID | Section | Severity | Status | Deviation | Fix direction |
|----|---------|----------|--------|-----------|---------------|
| PC-30 | `architecture-§2` | MUST | open | `NewAddon("PrettyChat", "AceConsole-3.0")` (`core/PrettyChat.lua:8`) does **not** pass the `ns` table as the first arg, so the bootstrap namespace and the AceAddon object are separate objects. | Pass `ns` as the first arg (`NewAddon(ns, addonName, "AceConsole-3.0")`); then **reclaim the printer** (`ns.Print = ns.Util.print` / equivalent) immediately after, since AceConsole's `:Print` mixin will now land on `ns` and would otherwise clobber the cyan-tagged printer (architecture-§2, anti-pattern #36). Update the AceAddon test mock to stamp the colliding `:Print`. |
| PC-31 | `documentation-§2`, `documentation-§6`, #34 | MUST | open | Root `CLAUDE.md` has no `## Standards compliance (read first)` section (the substance lives inside a `## Non-negotiable guardrails` bullet) and the H1 is `# CLAUDE.md`, not `# CLAUDE.md — Ka0s Pretty Chat`. | Add the mandated `## Standards compliance (read first)` section verbatim in substance (documentation-§6 canonical wording) and retitle the H1. Keep the accepted-deviation bullets. |
| PC-32 | `documentation-§3`, `documentation-§6`, #34 | MUST | open | `docs/agent-context.md` `## Hard rules` does **not** open with the "Conform to the Ka0s WoW Addon Standard" rule and the file contains **no reference to the standards repo at all** (0 occurrences of the URL). Two of the four mandated reference locations are missing. | Add, as the **first** `## Hard rules` bullet, the canonical conform-to-the-standard rule with the repo URL, pointing back to the `CLAUDE.md` "Standards compliance" section (documentation-§6 #4). |
| PC-33 | `toc-file-§5`, #28 | MUST | open (partly documented) | The TOC file-listing section order is `# Libraries → # Core → # Defaults → # Locales → # GlobalStrings → # Modules → # Settings`, diverging from the canonical `Libraries → Locales → Core → Defaults → Modules → Settings` — `# Locales` sits after Core/Defaults and a non-canonical `# GlobalStrings` section is inserted (`PrettyChat.toc:15-57`). | Move the `# Locales` section to immediately after `# Libraries` (verify `enUS.lua` has no earlier-load dependency — it does not). The `# GlobalStrings` section is inherent to the documented generated-data exception (PC-25); keep it, but note the ordering exception alongside PC-25. *(The standard's own `layout-§1` load order lists `defaults → locales`, which conflicts with `toc-file-§5`'s section order; flag to the user — this may be a signal the standard should reconcile the two.)* |
| PC-34 | `events-frames-taint-§8`, `slash-commands-§4`, `debug-logging-§4`, #35 | MUST | open | The shared chat printer `ns.Print` (`core/PrettyChat.lua:14-16`) and the debug sink `ns.Debug` (`core/DebugLog.lua:306-310`) build lines with raw `..` / `string.format` and no secret-safe stringifier. Non-compliant even though the addon never handles a combat-protected "secret" today. | Add `ns.IsConcatSafe` / `ns.SafeToString` (probe via `table.concat`, not `..`) to `core/Util.lua`; route both `ns.Print` and `ns.Debug` line-building through it. |
| PC-35 | `events-frames-taint-§8` | MUST | open | `modules/Override.lua` `Test()` and its label helpers write directly to `DEFAULT_CHAT_FRAME:AddMessage(PREFIX .. …)` (`Override.lua:221,223,252-289`), bypassing the single shared `ns.Print` seam; `settings/Schema.lua:187` also hand-writes a `[schema]` tag through `ns.Print` with pre-concatenated colour codes. | Funnel every chat line through `ns.Print` (once it is secret-safe, PC-34). Have `Test()` call `ns.Print(...)` instead of `DEFAULT_CHAT_FRAME:AddMessage(PREFIX .. ...)`; the printer prepends the tag and secret-stringifies args. |
| PC-36 | `documentation-§1` (item 6) | MUST | open | README `### Settings panel` presents the subcategories as a **bulleted list** (`README.md:46-58`) rather than the mandated **Tab \| Covers** table, one row per settings subcategory. | Convert the bullet list to a `Tab | Covers` table (one row per subcategory: General, Loot, Currency, Money, Reputation, Experience, Honor, Tradeskill, Misc). Per-panel prose MAY follow the table. |
| PC-39 | `savedvariables-§2`, `layout-§1` | SHOULD | open | The AceDB **profile** defaults table is declared inline in `core/PrettyChat.lua:18-29` rather than in `defaults/Profile.lua` (which does not exist); `defaults/Defaults.lua` holds the per-string default values. | Move the `profile` defaults table into `defaults/Profile.lua` (the `layout-§1`/`savedvariables-§2` home); keep `defaults/Defaults.lua` as the reference-data table it already is. |
| PC-38 | `slash-commands-§4` | SHOULD | open | Help header renders `v1.4.0 — slash commands (…)` with an inserted em-dash (`settings/Slash.lua:64`), vs the canonical `v<version> slash commands (…)`. | Drop the ` — ` so the header matches the canonical format string. |
| PC-37 | `packaging` | SHOULD | open | `.pkgmeta` ignores `docs`/`tests`/lockfiles but not the dev-only GlobalStrings source assets — `GlobalStrings/GlobalStrings.lua` (~1.6 MB source dump), `GlobalStrings/split_globalstrings.py`, `GlobalStrings/GlobalStrings.toc`, `GlobalStrings/README.md` — so the shipped package would carry ~1.6 MB of build-time-only data. | Add ignore entries for the GlobalStrings source dump, the splitter `.py`, the sub-`.toc`, and the sub-`README.md`, keeping only the runtime `GlobalStrings_0NN.lua` chunks in the package. |
| PC-10 | `toc-file-§1` | MUST | documented | `X-Wago-ID` is absent (`PrettyChat.toc`) although the addon is published (has `X-Curse-Project-ID: 919766`); `toc-file-§1` requires both once published anywhere. Recorded in `CLAUDE.md` as intentionally omitted "until a real Wago id is available (do not commit a placeholder)." | Re-confirm with the user: add `## X-Wago-ID` once a real Wago listing exists, or keep as an accepted deviation. Do not commit a placeholder. |
| PC-23 | `options-ui-§6` | SHOULD | documented | The per-string editor uses a bespoke 40/60 three-row layout (`settings/Panel.lua:398-528`) instead of the schema-driven 50/50 two-column grid. Justified in a code comment: the right column holds full colour-escaped format strings needing extra width. | Keep as a documented, in-code-justified deviation, or migrate to the paired grid with a `wide = true` full-width break for the editor rows. Re-confirm the justification with the user. |
| PC-25 | `layout-§1`, #25 (adjacent) | MUST | documented | `GlobalStrings/` is a generated-data folder at the **repo root**, outside the modular `core/defaults/settings/locales/modules` skeleton. Recorded in `CLAUDE.md` as an accepted generated-data exception (the modular layout has no home for bulk generated reference data). | Keep as the documented root exception, or relocate the generated chunks under a typed subfolder (e.g. `defaults/globalstrings/`) if the user prefers to remove the exception. |
| PC-27 | `toc-file-§1` | SHOULD | documented | `## Title:` carries rainbow `|cRRGGBB…|r` colour escapes and `## Author:` uses `aDd1kTeD2Ka0s` casing, vs the plain `Ka0s Pretty Chat` / `add1kted2ka0s` forms. Recorded in `CLAUDE.md` as the addon's brand mark. | Keep as the documented brand-mark deviation, or normalise. User's call. |
| PC-28 | `architecture-§1`, `naming-cheatsheet` | SHOULD | open | The namespace upvalue is lowercase `ns` throughout; the standard convention is `NS` (`local addonName, NS = ...`). | Optional rename to `NS` across all files for suite consistency (mechanical, test-covered). Low priority. |

## Summary counts

- **MUST** failures/partials: **8** — PC-30, PC-31, PC-32, PC-33, PC-34, PC-35, PC-36, and PC-10 (documented).
- **SHOULD** failures/partials: **7** — PC-37, PC-38, PC-39, PC-28, and PC-23 / PC-25 / PC-27 (documented).
- Of the 15, **7 are open MUST/SHOULD items** the addon has not addressed (PC-30/31/32/33/34/35/36 + PC-37/38/39/28), and **5 are pre-recorded intentional deviations** (PC-10, PC-23, PC-25, PC-27) plus the naming nit (PC-28) surfaced for re-confirmation against v2.7.0.

Note: PC-25 is counted once (layout) — it also drives the extra `# GlobalStrings` TOC section noted under PC-33.
